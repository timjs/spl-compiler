// Calculate the sum of a list of integers
Int sum ([Int] list)
{
	Int x = 0;// Have to be declared
	[Int] xs = [];

    match list
    case ([])
        return 0;
    case (x:xs)
        return x + sum(xs);
}

// vim: ft=c syn=spl

