// Calculate the sum of a list of integers
Int sum ([Int] list)
{
    Int   x  = 0;
    [Int] xs = [];

    match list
    case ([])
        return 0;
    case (x:xs)
        return x + sum(xs);
}

Void main () {
        print(sum(1:3:5:[]));
}

// vim: syn=spl

