// First the recursive version.
Int facR (Int n) {
  if (n < 2)
    return 1;
  else
    return n * facR(n - 1);
}

// A main function to check the results
Void main () {
  Int r = facR(4);
  print(r);
}

// vim: ft=c syn=spl
