// First the recursive version.
Int facR (Int n) {
  if (n < 2)
    return 1;
  else
    return n * facR(n - 1);
}

// A main function to check the results
Void main () {
  print(facR(4));
}

// vim: ft=c syn=spl
